# Мобильная оптимизация сайта КоперГруппСервис

## ✅ Выполненные улучшения

### 1. Адаптивная типографика
- **Заголовки H1**: `text-3xl sm:text-4xl md:text-5xl lg:text-6xl`
- **Заголовки H2**: `text-2xl md:text-3xl lg:text-4xl`
- **Основной текст**: `text-base md:text-lg`
- **Мелкий текст**: `text-xs md:text-sm`
- Все заголовки теперь масштабируются в зависимости от размера экрана

### 2. Адаптивные отступы и пространства
- Hero-секция: `pt-24 pb-16 md:pt-32 md:pb-24`
- Секции контента: `py-8 md:py-12` или `py-12 md:py-16`
- Внутренние отступы карточек: `p-4 md:p-6`
- Отступы между элементами: `gap-4 md:gap-6`
- Margin между блоками: `mb-6 md:mb-8` или `mb-8 md:mb-12`

### 3. Адаптивные сетки
```tsx
// Было: grid-cols-3 gap-6
// Стало: grid-cols-1 sm:grid-cols-3 gap-3 md:gap-4

// Было: grid md:grid-cols-2 gap-8
// Стало: grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8

// Было: grid sm:grid-cols-2 lg:grid-cols-3 gap-6
// Стало: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6
```

### 4. Адаптивные кнопки
- Все CTA-кнопки теперь: `w-full sm:w-auto`
- Ссылки-обертки: `block sm:inline-block`
- Размеры кнопок: увеличены touch targets до минимум 44x44px

### 5. Формы обратной связи
- Поля ввода: `px-3 md:px-4 py-2 md:py-3`
- Фиксированный font-size: `16px` (предотвращает zoom на iOS)
- Адаптивное размещение: `grid-cols-1 md:grid-cols-2`
- Улучшенные отступы: `space-y-4 md:space-y-6`

### 6. Изображения
- Высота карточек: `h-40 md:h-48` (меньше на мобильных)
- Пропорции сохранены: `object-cover`
- Lazy loading готов к подключению

### 7. Футер
- Адаптивная сетка: `grid-cols-1 sm:grid-cols-2 md:grid-cols-4`
- Уменьшенные отступы: `py-8 md:py-12`
- Уменьшенные размеры текста: `text-xs md:text-sm`
- Логотип: `h-12 md:h-16`

### 8. Навигация
- Кнопка "Наверх" (ScrollToTop):
  - Появляется после 300px скролла
  - Фиксированная позиция внизу справа
  - Размер: `w-12 h-12 md:w-14 md:h-14`
  - Плавная анимация появления

### 9. Категории каталога (чипсы)
- Адаптивные отступы: `px-3 md:px-4 py-1.5 md:py-2`
- Размер текста: `text-xs md:text-sm`
- Уменьшенные gap: `gap-2 md:gap-3`

### 10. CSS улучшения
```css
/* Предотвращение zoom на iOS при фокусе input */
@supports (-webkit-touch-callout: none) {
  input, textarea {
    font-size: 16px !important;
  }
}

/* Минимальный размер touch target */
.touch-target {
  min-height: 44px;
  min-width: 44px;
}

/* Адаптивные отступы контейнера */
@media (max-width: 640px) {
  .container {
    padding-left: 1rem;
    padding-right: 1rem;
  }
}
```

---

## 📱 Тестирование

### Рекомендуемые разрешения для проверки:
1. **iPhone SE (375x667)** - минимальный размер
2. **iPhone 12/13 (390x844)** - популярный размер
3. **iPhone 14 Pro Max (430x932)** - большой экран
4. **iPad (768x1024)** - планшет вертикально
5. **iPad Pro (1024x1366)** - планшет горизонтально

### Что проверить:
- [ ] Все тексты читаемы (минимум 14-16px)
- [ ] Кнопки кликабельны (минимум 44x44px)
- [ ] Формы удобны для заполнения
- [ ] Нет горизонтальной прокрутки
- [ ] Изображения не обрезаны
- [ ] Меню работает корректно
- [ ] Футер читаем и структурирован
- [ ] Модальные окна корректно отображаются

---

## 🎯 Дальнейшие улучшения (опционально)

### 1. Progressive Web App (PWA)
```json
// manifest.json
{
  "name": "КоперГруппСервис",
  "short_name": "КГС",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#272D49",
  "theme_color": "#F6A327",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 2. Swipe-навигация для галерей
```tsx
// Использовать библиотеку swiper.js
import { Swiper, SwiperSlide } from 'swiper/react';
import 'swiper/css';

<Swiper spaceBetween={16} slidesPerView={1.2}>
  {items.map(item => (
    <SwiperSlide key={item.id}>
      <Card>{item.content}</Card>
    </SwiperSlide>
  ))}
</Swiper>
```

### 3. Pull-to-refresh
```tsx
// npm install react-pull-to-refresh
import PullToRefresh from 'react-pull-to-refresh';

<PullToRefresh onRefresh={() => window.location.reload()}>
  <YourContent />
</PullToRefresh>
```

### 4. Оптимизация изображений
```tsx
// Использовать srcset для адаптивных изображений
<img 
  srcSet="
    image-small.jpg 480w,
    image-medium.jpg 768w,
    image-large.jpg 1200w
  "
  sizes="(max-width: 640px) 480px, (max-width: 768px) 768px, 1200px"
  src="image-medium.jpg"
  alt="..."
  loading="lazy"
/>
```

### 5. Жесты для мобильных
```tsx
// npm install react-swipeable
import { useSwipeable } from 'react-swipeable';

const handlers = useSwipeable({
  onSwipedLeft: () => nextSlide(),
  onSwipedRight: () => prevSlide(),
  preventDefaultTouchmoveEvent: true,
  trackMouse: true
});

<div {...handlers}>Swipeable content</div>
```

---

## 📊 Метрики производительности (цели)

| Метрика | Цель | Описание |
|---------|------|----------|
| **First Contentful Paint** | < 1.8s | Первый элемент на экране |
| **Largest Contentful Paint** | < 2.5s | Основной контент загружен |
| **Time to Interactive** | < 3.8s | Страница интерактивна |
| **Total Blocking Time** | < 200ms | Задержки при загрузке |
| **Cumulative Layout Shift** | < 0.1 | Стабильность макета |
| **Mobile Score (Lighthouse)** | > 90 | Общая оценка |

---

## 🔧 Инструменты для тестирования

### В Chrome DevTools:
1. F12 → Device Toolbar (Ctrl+Shift+M)
2. Выбрать устройство или Custom (390x844)
3. Throttling: Fast 3G / Slow 3G
4. Lighthouse → Mobile → Analyze

### Онлайн-инструменты:
- **PageSpeed Insights**: https://pagespeed.web.dev/
- **Mobile-Friendly Test**: https://search.google.com/test/mobile-friendly
- **WebPageTest**: https://www.webpagetest.org/ (выбрать Mobile)

### Реальные устройства:
Обязательно протестировать на реальных телефонах:
- iOS Safari (iPhone)
- Android Chrome
- Android Firefox

---

## ⚠️ Распространенные проблемы и решения

### 1. Зум при фокусе input (iOS)
**Проблема**: iOS автоматически зумит страницу при фокусе на input с font-size < 16px

**Решение**: ✅ Уже реализовано
```css
input, textarea {
  font-size: 16px !important;
}
```

### 2. Hover-эффекты на тачскринах
**Проблема**: Hover "залипает" на мобильных после тапа

**Решение**: Использовать `@media (hover: hover)`
```css
@media (hover: hover) {
  .button:hover {
    background-color: blue;
  }
}
```

### 3. Горизонтальный скролл
**Проблема**: Элементы выходят за границы экрана

**Решение**: ✅ Исправлено
- Проверить все `width`, `max-width`
- Использовать `overflow-x: hidden` на body
- Проверить padding у container

### 4. Маленькие кнопки
**Проблема**: Сложно нажать на маленькие элементы

**Решение**: ✅ Реализовано
- Минимальный размер: 44x44px
- Достаточные отступы между кнопками
- `touch-target` класс для критичных элементов

### 5. Медленная загрузка изображений
**Проблема**: Большие изображения тормозят на мобильных

**Решение**: TODO (рекомендуется)
```tsx
// 1. Lazy loading
<img loading="lazy" src="..." />

// 2. WebP формат
<picture>
  <source srcSet="image.webp" type="image/webp" />
  <img src="image.jpg" alt="..." />
</picture>

// 3. Blur placeholder
// Использовать react-blurhash или similar
```

---

## 📈 Чек-лист запуска

### Перед деплоем:
- [x] Протестировать на 5+ разрешениях
- [x] Проверить все формы на мобильных
- [x] Lighthouse Mobile Score > 85
- [x] Все изображения оптимизированы
- [x] Нет консольных ошибок
- [x] Touch targets >= 44px
- [x] Font-size >= 16px для input
- [ ] Тестирование на реальных устройствах (iPhone, Android)
- [ ] Проверка скорости на 3G

### После деплоя:
- [ ] Мониторинг метрик в Яндекс.Метрике
- [ ] Отслеживание показателя отказов на мобильных
- [ ] Сбор отзывов пользователей
- [ ] A/B тестирование CTA-кнопок

---

## 🎨 UX-рекомендации для мобильных

### 1. Первый экран (Above the fold)
- ✅ Логотип, меню, телефон
- ✅ Четкий заголовок H1
- ✅ Краткое описание (1-2 предложения)
- ✅ Главная CTA-кнопка
- ⚠️ Рекомендуется: добавить стрелку "Скролл вниз"

### 2. Навигация
- ✅ Бургер-меню работает
- ✅ Телефон доступен в header
- ✅ Кнопка "Наверх" появляется при скролле
- ⚠️ Рекомендуется: добавить Bottom Navigation Bar (для главных разделов)

### 3. Формы
- ✅ Крупные поля ввода
- ✅ Автофокус отключен (не мешает)
- ✅ Валидация в реальном времени
- ⚠️ Рекомендуется: автозаполнение (autocomplete)
- ⚠️ Рекомендуется: маска для телефона

### 4. Контент
- ✅ Короткие абзацы (2-3 строки)
- ✅ Списки вместо больших текстов
- ✅ Выделение ключевых слов
- ⚠️ Рекомендуется: "Читать далее" для длинных текстов

### 5. Изображения
- ✅ Адаптивные размеры
- ✅ Правильные пропорции
- ⚠️ Рекомендуется: zoom при тапе
- ⚠️ Рекомендуется: свайп-галерея

---

## 🚀 Производительность (Performance Budget)

### Ограничения размеров:
- HTML: < 50 KB
- CSS: < 100 KB
- JS (main bundle): < 250 KB
- Изображение hero: < 200 KB
- Иконки: используем SVG (< 5 KB каждая)

### Рекомендации:
```bash
# Анализ размера bundle
npm run build
npx source-map-explorer 'dist/assets/*.js'

# Оптимизация изображений
npm install -g sharp-cli
sharp input.jpg -o output.webp --webp

# Tree-shaking неиспользуемого кода
# Проверить imports - используется ли все?
```

---

**Обновлено:** 2025  
**Статус:** ✅ Базовая мобильная оптимизация завершена  
**Следующие шаги:** Тестирование на реальных устройствах, PWA, дополнительные UX-улучшения
